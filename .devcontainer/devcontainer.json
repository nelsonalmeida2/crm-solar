{
  "name": "django-dev",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "forwardPorts": [8000],
  "postCreateCommand": "pip install django && django-admin startproject crm .",
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": []
    }
  }
}
